# Reference continuation — September 14, 2026

Authorization: user selected one comprehensive PR reconstructing office and agents to match the supplied photo, including rendering fixes.

Prior state: .img2threejs/state.json; retained without resetting any checklist or history. `forge/next.py` reports reference-suitability pending. The prior reference and sofa ledger remain historical evidence. The generic single-object generator rejects multi-object room photographs; this scene uses the skill's authorized direct Three.js continuation route. Its automated pipeline is not claimed to pass.

Current reference: user-uploaded 1000009043.png, retained locally and excluded from Git. Visible identity: herringbone floor, cutaway cream walls, thin black glazing, two navy/green lounges with circular stone tables and rugs, central desk islands, glass meeting room, bookcase/café, black marble reception, living wall, suited adult humans. Hidden rear rooms, dimensions, faces and reverse surfaces are inferred. No reference pixels are used as room geometry.

Quality contract: full-room architecture is ultra-complex, hybrid furnishing/character domain. Critical systems are room composition, furnishing silhouette/placement, adult suited anatomy, oak/stone/cloth material separation, and camera/UI proportions. Every system requires an actual GPU render before visual acceptance. Current cloud browser reports WebGPU unavailable and a null WebGL context; this is a validation blocker, not a passed fidelity gate.

Changed strategy: replace the old gym-based plan with a shared reference layout consumed by both visible furniture and simulation task slots; build a dedicated procedural room and tailored skeletal character. Retain CPU skinning on WebGL2 and GPU instancing on WebGPU. Validate topology, skeletal motion and task access separately from visual fidelity.

Structural review: procedural mesh export and software rasterization inspected.
Corrections: fixed herringbone orientation, moved reception to right foreground,
aligned green/navy lounge positions, replaced lounge task chairs with upholstered
armchairs, removed horizontal pots from the living wall, added perimeter rooms
and planted dividers. Adult mesh clones have finite deformation in Walk and Sit.
Fourteen tests pass. Browser/GPU comparison remains blocked; no fidelity score or
pipeline acceptance was assigned. Continued asset work is an approximation, not
an identical reproduction of the photograph.

The locally retained `software-geometry-audit.png` is the last structural render of the actual geometry,
with a staged 24-person pose sample. It omits transparent glass, shadows and GPU
material response; it is not the live 100-agent simulation or a fidelity pass.
